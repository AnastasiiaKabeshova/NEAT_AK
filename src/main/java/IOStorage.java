
import com.thoughtworks.xstream.XStream;

public class IOStorage {
    
    private static final String DEFAULT_FILE_NAME = "genotype.xml";
    private String fileName = DEFAULT_FILE_NAME;
    private XStream xstream = new XStream();
    
}
